extends Node

class_name COLOR

# default setting
const NONE = Color(0, 0, 0, 1)

# elemental colours for modulation (eg: self_modulate = COLOR.FREEZE
const FREEZE = Color(0 , 0.7, 1, 1)
const FIRE = Color(1, 0.2, 0, 1)
